export default function App() {
  return (
    <div style={{
      minHeight: '100vh',
      background: '#111',
      color: '#ccc',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      fontFamily: 'monospace'
    }}>
      <div>
        <h1 style={{ color: '#e8c84a' }}>cladding-configurator</h1>
        <p>Working repo initialized.</p>
        <p>Next step: push full geometry engine + measured angle mode.</p>
      </div>
    </div>
  )
}
